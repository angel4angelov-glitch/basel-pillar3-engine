"""Paths, constants, and logging setup.

Stub for chunk 0.1 (scaffold only). Implemented in chunk 0.2 — port the
boe-rag ``config.py`` pattern (``Paths``, constants, ``setup_logging()``).
"""

# TODO(0.2): Paths, constants, setup_logging().
