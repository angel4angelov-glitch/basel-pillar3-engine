"""DI orchestrator wiring boxes 3 -> 4 -> 5 -> 7.

Stub for chunk 0.1 (scaffold only). Implemented in chunk 1.8 — engines and LLM
are injected so the pipeline is stub-testable with no network/key.
"""

# TODO(1.8): inject engines + LLM; wire extraction -> mapping -> reconcile -> store.
