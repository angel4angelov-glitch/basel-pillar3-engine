"""Command-line entry point for isda-p3.

Stub for chunk 0.1 (scaffold only). Subcommands (run/ingest/review/analytics/
benchmark) are wired in later chunks. ``main()`` exists so the console-script
entry point imports and runs.
"""


def main() -> None:
    """Entry point. Prints help until subcommands land in later chunks."""
    print("isda-p3: Basel Pillar 3 extraction & benchmarking (scaffold).")
    print("Subcommands (run/ingest/review/analytics/benchmark) not yet wired.")
