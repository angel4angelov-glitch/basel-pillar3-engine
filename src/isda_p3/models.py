"""Domain types: frozen dataclasses + StrEnums (Decimal-based).

Stub for chunk 0.1 (scaffold only). Implemented in chunk 0.3 — the two basis
axes, ``MappingDecision``, ``Provenance``, etc. (see plan Domain-model section).
"""

# TODO(0.3): StrEnums + frozen dataclasses (Decimal, provenance threading).
